TODO
----

* Not testing a POST which triggers process_multipart_form_data(). Or at
  least doesn't seem to be doing what was intended. The unit test function
  is 'tests/test_application.py:test_application_encode_multipart()'.
